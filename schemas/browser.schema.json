{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/kevcooper/har/refs/heads/main/schemas/browser.schema.json",
  "title": "browser",
  "description": "Browser used for session",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the browser used to export the log."
    },
    "version": {
      "type": "string",
      "description": "Version of the browser used to export the log."
    },
    "comment": {
      "type": "string",
      "description": "A comment provided by the user or the application."
    }
  },
  "required": [
    "name",
    "version"
  ]
}